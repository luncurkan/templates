services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - DATABASE_URL=sqlite+aiosqlite:///./data/data.db
    volumes:
      - ./data:/app/data
